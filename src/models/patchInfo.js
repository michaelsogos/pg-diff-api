class PatchInfo {
    constructor(filename, filepath, version, name) {
        this.filename = filename;
        this.filepath = filepath;
        this.version = version;
        this.name = name;
    }
}

module.exports = PatchInfo;
